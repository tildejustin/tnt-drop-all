{
  "schemaVersion": 1,
  "id": "${archives_base_name}",
  "version": "${version}+${minecraft_version}",
  "name": "TNT 100% Drop Rate",
  "description": "Makes tnt drop rate 100% for versions where it isn't (targeting 1.8.9)",
  "authors": [
    "tildejustin"
  ],
  "license": "MIT",
  "icon": "assets/modid/icon.png",
  "environment": "*",
  "mixins": [
    "tntmod.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.12.12"
  }
}
